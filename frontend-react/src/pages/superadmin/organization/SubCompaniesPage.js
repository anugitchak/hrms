import React, { useState, useEffect } from"react"; import api from"../../../api/axios"; const SubCompaniesPage = () => { const [subCompanies, setSubCompanies] = useState([]); const [countries, setCountries] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(null); const [searchQuery, setSearchQuery] = useState(""); const [selectedSubCompany, setSelectedSubCompany] = useState(null); // Modals
const [isAddModalOpen, setIsAddModalOpen] = useState(false); const [isEditModalOpen, setIsEditModalOpen] = useState(false); const [isDeleteModalOpen, setIsDeleteModalOpen] = useState(false); // Form State
const [formData, setFormData] = useState({ name:"", code:"", country_id:"", is_active: true }); const [formError, setFormError] = useState(null); const [isSubmitting, setIsSubmitting] = useState(false); const [successMessage, setSuccessMessage] = useState(null); useEffect(() => { fetchData(); }, []); const fetchData = async () => { setLoading(true); try { const [subCompaniesRes, countriesRes] = await Promise.all([ api.get("/sub-companies"), api.get("/countries") ]); setSubCompanies(subCompaniesRes.data); setCountries(countriesRes.data); setError(null); } catch (err) { console.error("Failed to fetch data", err); setError("Failed to load sub-companies."); } finally { setLoading(false); } }; const handleSearch = (e) => { setSearchQuery(e.target.value); }; const openAddModal = () => { setFormData({ name:"", code:"", country_id:"", is_active: true }); setFormError(null); setIsAddModalOpen(true); }; const openEditModal = (subCompany) => { setSelectedSubCompany(subCompany); setFormData({ name: subCompany.name, code: subCompany.code, country_id: subCompany.country_id, is_active: subCompany.is_active }); setFormError(null); setIsEditModalOpen(true); }; const openDeleteModal = (subCompany) => { setSelectedSubCompany(subCompany); setIsDeleteModalOpen(true); }; const closeModals = () => { setIsAddModalOpen(false); setIsEditModalOpen(false); setIsDeleteModalOpen(false); setSelectedSubCompany(null); setFormError(null); }; const handleAddSubmit = async (e) => { e.preventDefault(); if (!formData.name.trim() || !formData.code.trim() || !formData.country_id) { setFormError("All fields are required."); return; } setIsSubmitting(true); try { const response = await api.post("/sub-companies", formData); setSubCompanies([...subCompanies, response.data.sub_company].sort((a, b) => a.name.localeCompare(b.name))); setSuccessMessage("Sub-company created successfully!"); setTimeout(() => setSuccessMessage(null), 3000); closeModals(); } catch (err) { console.error("Failed to create sub-company", err); setFormError(err.response?.data?.message ||"Failed to create sub-company."); } finally { setIsSubmitting(false); } }; const handleEditSubmit = async (e) => { e.preventDefault(); if (!formData.name.trim() || !formData.code.trim() || !formData.country_id) { setFormError("All fields are required."); return; } setIsSubmitting(true); try { const response = await api.put(`/sub-companies/${selectedSubCompany.id}`, formData); setSubCompanies(subCompanies.map(sc => sc.id === selectedSubCompany.id ? response.data.sub_company : sc) .sort((a, b) => a.name.localeCompare(b.name))); setSuccessMessage("Sub-company updated successfully!"); setTimeout(() => setSuccessMessage(null), 3000); closeModals(); } catch (err) { console.error("Failed to update sub-company", err); setFormError(err.response?.data?.message ||"Failed to update sub-company."); } finally { setIsSubmitting(false); } }; const handleDeleteSubmit = async () => { setIsSubmitting(true); try { await api.delete(`/sub-companies/${selectedSubCompany.id}`); setSubCompanies(subCompanies.filter(sc => sc.id !== selectedSubCompany.id)); setSuccessMessage("Sub-company deleted successfully!"); setTimeout(() => setSuccessMessage(null), 3000); closeModals(); } catch (err) { console.error("Failed to delete sub-company", err); setFormError(err.response?.data?.message ||"Failed to delete sub-company. It may have associated employees."); } finally { setIsSubmitting(false); } }; const filteredSubCompanies = subCompanies.filter(sc => sc && sc.name && sc.name.toLowerCase().includes(searchQuery.toLowerCase()) ); return ( <div className="p-8"> {successMessage && ( <div className="fixed top-4 right-4 bg-green-500 text-white p-4 rounded-lg shadow-lg z-50"> {successMessage} </div> )} {/* Header */} <div className="flex justify-between items-center mb-6"> <div> <h1 className="text-3xl font-extrabold text-black font-paperlogy">Sub-Companies Management</h1> <p className="text-sm text-gray-900 mt-1">Manage sub-companies across countries</p> </div> <div className="flex gap-3"> <button onClick={fetchData} className="btn-secondary transition-colors text-sm font-medium" > Refresh </button> <button onClick={openAddModal} className="btn-primary text-sm font-medium" > + Add Sub-Company </button> </div> </div> {/* Search */} <div className="mb-6"> <input type="text" placeholder="Search by sub-company name..." value={searchQuery} onChange={handleSearch} className="p-3 border-2 border-black rounded-lg outline-none w-full max-w-xs bg-white dark:bg-gray-800 text-gray-900 dark:text-white focus:ring-4 focus:ring-brand-500 font-medium" /> </div> {/* Table */} {loading ? (
  <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
    {[1,2,3].map(i => (<div key={i} className="card p-5 animate-pulse"><div className="h-5 bg-gray-200 rounded w-3/4 mb-3"></div><div className="h-3 bg-gray-100 rounded w-full mb-2"></div></div>))}
  </div>
) : error ? (
  <div className="p-8 text-center text-red-500">{error}</div>
) : filteredSubCompanies.length === 0 ? (
  <div className="card p-16 text-center"><p className="font-bold text-gray-500">No sub-companies found.</p></div>
) : (
  <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
    {filteredSubCompanies.map(subCompany => {
      const palette = ["from-purple-500 to-purple-700","from-blue-500 to-blue-700","from-teal-500 to-teal-700","from-pink-500 to-pink-700","from-orange-500 to-orange-700","from-indigo-500 to-indigo-700"];
      const bg = palette[(subCompany.name?.charCodeAt(0)||0) % palette.length];
      return (
        <div key={subCompany.id} className={`card p-5 flex flex-col gap-3 border-2 hover:-translate-y-1 hover:shadow-lg transition-all duration-200 ${subCompany.is_active ? "border-black/10" : "border-red-200 bg-red-50/30"}`}>
          <div className="flex items-start justify-between">
            <div className="flex items-center gap-3">
              <div className={`w-11 h-11 rounded-2xl bg-gradient-to-br ${bg} text-white flex items-center justify-center text-lg font-black border-2 border-black shadow-[2px_2px_0px_black] shrink-0`}>{subCompany.name?.charAt(0).toUpperCase()}</div>
              <div><h3 className="font-extrabold text-black text-sm">{subCompany.name}</h3><p className="text-xs font-bold text-gray-500 mt-0.5">{subCompany.code}</p></div>
            </div>
            <span className={`px-2.5 py-1 text-[10px] font-bold border rounded-full shrink-0 ${subCompany.is_active ? "bg-green-100 text-green-700 border-green-300" : "bg-red-100 text-red-700 border-red-300"}`}>{subCompany.is_active ? "Active" : "Inactive"}</span>
          </div>
          <div className="flex items-center gap-2 bg-gray-50 rounded-xl p-2.5 border border-black/10">
            <span className="text-xs font-extrabold text-gray-400 uppercase tracking-wider">Country</span>
            <span className="ml-auto text-xs font-black text-black">{subCompany.country?.name || "N/A"}</span>
          </div>
          <div className="flex items-center gap-2 pt-2 border-t-2 border-black/5">
            <button onClick={() => openEditModal(subCompany)} className="flex-1 text-xs font-bold py-1.5 rounded-lg border-2 border-blue-300 text-blue-600 bg-blue-50 hover:bg-blue-100 transition-all">Edit</button>
            <button onClick={() => openDeleteModal(subCompany)} className="flex-1 text-xs font-bold py-1.5 rounded-lg border-2 border-black text-black bg-white hover:bg-gray-100 transition-all shadow-[2px_2px_0px_black]">Delete</button>
          </div>
        </div>
      );
    })}
  </div>
)} {/* Add Modal */} {isAddModalOpen && ( <div className="fixed inset-0 bg-black/60 flex items-center justify-center z-50 z-50"> <div className="bg-white dark:bg-gray-800 rounded-lg p-6 w-full max-w-md"> <h2 className="text-xl font-bold mb-4 text-gray-900 dark:text-white">Add Sub-Company</h2> {formError && <div className="mb-4 p-3 bg-red-100 dark:bg-red-900/30 text-red-700 dark:text-red-400 rounded">{formError}</div>} <form onSubmit={handleAddSubmit}> <div className="mb-4"> <label className="block text-sm font-medium mb-2 text-gray-700 dark:text-gray-300">Sub-Company Name</label> <input type="text" value={formData.name} onChange={(e) => setFormData({ ...formData, name: e.target.value })} className="w-full p-2 border border-gray-300 dark:border-gray-600 rounded bg-white dark:bg-gray-700 text-gray-900 dark:text-white" required /> </div> <div className="mb-4"> <label className="block text-sm font-medium mb-2 text-gray-700 dark:text-gray-300">Sub-Company Code</label> <input type="text" value={formData.code} onChange={(e) => setFormData({ ...formData, code: e.target.value.toUpperCase() })} placeholder="e.g., WOWL-IN" maxLength="20" className="w-full p-2 border border-gray-300 dark:border-gray-600 rounded bg-white dark:bg-gray-700 text-gray-900 dark:text-white" required /> </div> <div className="mb-4"> <label className="block text-sm font-medium mb-2 text-gray-700 dark:text-gray-300">Country</label> <select value={formData.country_id} onChange={(e) => setFormData({ ...formData, country_id: e.target.value })} className="w-full p-2 border border-gray-300 dark:border-gray-600 rounded bg-white dark:bg-gray-700 text-gray-900 dark:text-white" required > <option value="">Select Country</option> {countries.map(country => ( <option key={country.id} value={country.id}> {country.name} </option> ))} </select> </div> <div className="mb-4"> <label className="flex items-center text-sm text-gray-700 dark:text-gray-300"> <input type="checkbox" checked={formData.is_active} onChange={(e) => setFormData({ ...formData, is_active: e.target.checked })} className="mr-2" /> Active </label> </div> <div className="flex gap-2 justify-end"> <button type="button" onClick={closeModals} className="px-4 py-2 border border-gray-300 dark:border-gray-600 rounded text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700" disabled={isSubmitting} > Cancel </button> <button type="submit" className="px-4 py-2 btn-primary$3" disabled={isSubmitting} > {isSubmitting ?"Creating..." :"Create"} </button> </div> </form> </div> </div> )} {/* Edit Modal */} {isEditModalOpen && ( <div className="fixed inset-0 bg-black/60 flex items-center justify-center z-50 z-50"> <div className="bg-white dark:bg-gray-800 rounded-lg p-6 w-full max-w-md"> <h2 className="text-xl font-bold mb-4 text-gray-900 dark:text-white">Edit Sub-Company</h2> {formError && <div className="mb-4 p-3 bg-red-100 dark:bg-red-900/30 text-red-700 dark:text-red-400 rounded">{formError}</div>} <form onSubmit={handleEditSubmit}> <div className="mb-4"> <label className="block text-sm font-medium mb-2 text-gray-700 dark:text-gray-300">Sub-Company Name</label> <input type="text" value={formData.name} onChange={(e) => setFormData({ ...formData, name: e.target.value })} className="w-full p-2 border border-gray-300 dark:border-gray-600 rounded bg-white dark:bg-gray-700 text-gray-900 dark:text-white" required /> </div> <div className="mb-4"> <label className="block text-sm font-medium mb-2 text-gray-700 dark:text-gray-300">Sub-Company Code</label> <input type="text" value={formData.code} onChange={(e) => setFormData({ ...formData, code: e.target.value.toUpperCase() })} maxLength="20" className="w-full p-2 border border-gray-300 dark:border-gray-600 rounded bg-white dark:bg-gray-700 text-gray-900 dark:text-white" required /> </div> <div className="mb-4"> <label className="block text-sm font-medium mb-2 text-gray-700 dark:text-gray-300">Country</label> <select value={formData.country_id} onChange={(e) => setFormData({ ...formData, country_id: e.target.value })} className="w-full p-2 border border-gray-300 dark:border-gray-600 rounded bg-white dark:bg-gray-700 text-gray-900 dark:text-white" required > <option value="">Select Country</option> {countries.map(country => ( <option key={country.id} value={country.id}> {country.name} </option> ))} </select> </div> <div className="mb-4"> <label className="flex items-center text-sm text-gray-700 dark:text-gray-300"> <input type="checkbox" checked={formData.is_active} onChange={(e) => setFormData({ ...formData, is_active: e.target.checked })} className="mr-2" /> Active </label> </div> <div className="flex gap-2 justify-end"> <button type="button" onClick={closeModals} className="px-4 py-2 border border-gray-300 dark:border-gray-600 rounded text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700" disabled={isSubmitting} > Cancel </button> <button type="submit" className="px-4 py-2 btn-primary$3" disabled={isSubmitting} > {isSubmitting ?"Updating..." :"Update"} </button> </div> </form> </div> </div> )} {/* Delete Modal */} {isDeleteModalOpen && ( <div className="fixed inset-0 bg-black/60 flex items-center justify-center z-50 z-50"> <div className="bg-white dark:bg-gray-800 rounded-lg p-6 w-full max-w-md"> <h2 className="text-xl font-bold mb-4 text-gray-900 dark:text-white">Delete Sub-Company</h2> {formError && <div className="mb-4 p-3 bg-red-100 dark:bg-red-900/30 text-red-700 dark:text-red-400 rounded">{formError}</div>} <p className="mb-6 text-gray-700 dark:text-gray-300"> Are you sure you want to delete <strong>{selectedSubCompany?.name}</strong>? This action cannot be undone. </p> <div className="flex gap-2 justify-end"> <button onClick={closeModals} className="px-4 py-2 border border-gray-300 dark:border-gray-600 rounded text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700" disabled={isSubmitting} > Cancel </button> <button onClick={handleDeleteSubmit} className="px-4 py-2 btn-accent bg-red-500 hover:bg-red-600 text-white border-2 border-black shadow-[2px_2px_0px_rgba(0,0,0,1)]$3" disabled={isSubmitting} > {isSubmitting ?"Deleting..." :"Delete"} </button> </div> </div> </div> )} </div> ); }; export default SubCompaniesPage; 