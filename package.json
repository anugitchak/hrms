{
  "name": "hrms-workspace",
  "version": "1.0.0",
  "description": "HRMS Merged Workspace",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix frontend-react && npm install --prefix backend",
    "build:frontend": "npm run build --prefix frontend-react && node backend/scripts/copy-frontend.cjs",
    "start:backend": "php backend/artisan serve",
    "start:frontend": "npm start --prefix frontend-react",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "prod": "npm run build:frontend && npm run start:backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
