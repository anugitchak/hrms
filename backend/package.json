{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "copy:frontend": "node scripts/copy-frontend.cjs",
        "build:frontend": "npm run build --prefix ../frontend-react && npm run copy:frontend",
        "serve": "npm run build:frontend && php artisan serve",
        "serve:quick": "php artisan serve"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    }
}
